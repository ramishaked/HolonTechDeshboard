# דשבורד מדד בגרות-טק — חולון

## מה זה
קובץ HTML **יחיד** (`index.html`) שמתארח ב-GitHub Pages ומושך נתונים חיים
מגיליון גוגל מפורסם. אין DB, אין קבצי נתונים, אין שלב בנייה.

חי בכתובת: https://ramishaked.github.io/HolonTechDeshboard/

## לפני שנוגעים בקוד
`holon_live_spec.md` מכיל את כל ההחלטות שסוכמו — מבנה הגיליון, חוקי החישוב,
השפה העיצובית, ומה אסור לשבור. **אין צורך לדון מחדש במה שכתוב שם.**

**קרא את האינדקס שבראשו, ואז את הסעיפים הנוגעים למשימה** — לא את כולו.
המסמך הוא ~25K טוקנים; קריאה מלאה בכל משימה היא מס פתיחה כבד ומיותר.

**קריאה מלאה נדרשת** רק בשלושה מקרים: שינוי חוק חישוב (§3), הוספת תצוגה
חדשה, או נגיעה בשכבת הנתונים (הפרסר, `boot`, `GID_MAP`).

## כללי ברזל
1. **לא לשבור את העיצוב הקיים.** צבעים, פריסה, sidebar, גדלי פונט, מבנה
   הכרטיסים וסוגי הגרפים — אושרו והמשתמש התרגל אליהם. שינוי רק באישור מפורש.
2. **לא להמציא נתונים.** הכל מהגיליון, בזמן ריצה.
3. **קובץ אחד — באפליקציה.** `index.html` נשאר יחיד: לא לפצל ל-JS/CSS
   נפרדים, לא להוסיף שלב בנייה, ולא ליצור לו תלות בשום קובץ אחר בריפו.
   (`tools/` הוא אימות מקומי בלבד. GitHub Pages לא מגיש אותו, והדף לא
   יודע שהוא קיים.)
4. **חישוב עצמאי.** שורות `סה"כ עיר` שבגיליון אינן מקור — רק כלי הצלבה.
5. `זכאות למדד טק` הוא המדד. לא לסכום פיזיקה+מדמ"ח (יש חפיפה).

## מבנה הקוד ב-index.html
| אזור | תוכן |
|------|------|
| `<style>` | כל ה-CSS. משתני הצבע ב-`:root` |
| HTML | sidebar, 10 `section.view`, חלון "מקור הנתונים" |
| `<script>` | מלכודת שגיאות → helpers → 10 פונקציות `render*` → שכבת הנתונים |

**שכבת הנתונים** בסוף הסקריפט: `GID_MAP` → `csvParse` → `parseSheet` →
`boot()` → `applyPeriod()`. `BUILD` הוא מספר הגרסה שמוצג בסיידבר —
**להעלות אותו בכל שינוי**, אחרת אי אפשר לדעת איזו גרסה רצה בדפדפן.

### להתמצא בקובץ — grep במקום סריקה
הקובץ הוא ~4,500 שורות. **לחפש לפי שם פונקציה, לא לקרוא רצף.**

```
node tools/verify.mjs --map
```
מדפיס את כל הפונקציות שבקובץ, מקובצות לפי תחום (רינדור · תצוגה 8 · ערים
אחרות · עתיד טק · מדד ומכנה · בית ספר · טעינה · ניווט), עם `BUILD` ומספר
השורות. **נגזר מ-`index.html` בזמן ריצה.**

מפה כתובה ביד עמדה כאן קודם ו**התיישנה תוך שעה** — תצוגה נוספה ושלוש
פונקציות `nt*` הוסרו באותו יום. עם כמה סשנים שעובדים במקביל היא תהיה
שגויה רוב הזמן, ומפה שגויה גרועה מאין מפה. **לא להחזיר אותה.**

## מבנה הנתונים
```js
SCHOOLS = [{ name, note, partial, grades: { 'י': [...15 שדות] } }]
// 0 total  1 cs  2 ph  3 ma5  4 ht  5 cs_g  6 ph_g  7 ht_g
// 8 amat   9 ma5_g  10 ft  11 ft_g  12 ma3  13 ma4  14 en5
FULL = SCHOOLS.filter(s => !s.partial)   // דירוג ונשירה בלבד
```
- **מדדים עירוניים** → `SCHOOLS` (כל בית ספר בכל שכבה שיש לו נתונים)
- **דירוג בין בתי ספר + נשירה** → `FULL` (דורש י'–יב' מלאים)
- **כל מכנה עירוני עובר דרך `cityDenomByName(שכבה)`** — סכום שורות
  `כמות תלמידים`. מכנה עירוני חדש חייב לקרוא לה ולא לסכום `total` בעצמו.
- **המצבת אינה מכנה** — היא מספר ייחוס שמוצג בסוגריים ליד המדד
  (`cityDenomAlt` → `cityPctAlt` → `altSpan`, §3.2). אין מתג. המונה לא מושפע.
  **החריג היחיד: תצוגה 9** (חולון מול ערים אחרות), שם המצבת היא המכנה הראשי כי
  זה המכנה שכל שאר הרשויות נמדדו בו. ההיפוך מקומי לאותו כרטיס בלבד.
- `amat=1` — בחט"ב פיזיקה=מדמ"ח=זכאות, כלומר רישום קבוצת עמ"ט ולא בחירת
  מקצוע. מוצג כקו אחד, לא שלושה.

## גישה לתקופה שאינה הנבחרת (תצוגה 8)
`applyPeriod` מציב ב-`SCHOOLS`/`FULL`/`MATZEVET` את התקופה הנבחרת בלבד.
כל התקופות כבר בזיכרון ב-`ALLDATA` (נטענות במקביל בכל `boot`).

**לחשב על תקופה אחרת = `withPeriod(key, fn, filter?)` בלבד.** היא מחליפה את
הגלובלים זמנית, מריצה פונקציית חישוב **קיימת**, ומשחזרת ב-`finally`:
```js
const p10_2025 = withPeriod('תשפה-2', () => cityPct(0));
```
- **לא לכתוב גרסה מקבילה של נוסחה** שמקבלת תקופה כפרמטר. זו הסיבה שהמספרים
  בהשוואה השנתית זהים בהגדרה לאלה שבסקירה העירונית.
- **בתוך `fn` רק חישוב טהור.** לא `render*`, לא DOM, לא `new Chart` —
  דליפה של הגלובלים משבשת בשקט את כל התצוגות האחרות.
- `filter` (רשות) מצמצם את בתי הספר לתת-קבוצה.
- סדר כרונולוגי: `periodsSorted()`.
- **תצוגה 7 (עתיד טק) עושה זאת אחרת** — מעבירה `schools` כפרמטר
  (`ftCity(schools, g)`, `studentsAtGrade(schools, g)`). זו הדרך הנכונה לחישוב
  חדש וקצר; `withPeriod` הוא לכשצריך **בדיוק** את נוסחאות התצוגות הקיימות.

## השוואה שנתית (תצוגה 8 — שנייה בתפריט)
- **מספור התצוגות ≠ סדר התפריט.** "תצוגה 8" הוא מזהה לפי סדר בנייה; בסיידבר
  היא יושבת שנייה, אחרי הסקירה העירונית. לא לשנות מספור בעקבות שינוי בתפריט.
- **שאלה אחת: מה השתנה בין סוף השנה שעברה לסוף השנה הזו.** לא ציר זמן, לא מחזורים,
  לא מטריצה. הגרסה הקודמת (v16) ניסתה את כל אלה ונדחתה כמסובכת מדי להנהלה.
- `yyPeriods()` בוחרת את **שתי דגימות סוף-השנה האחרונות** (`half===2`). דגימת
  תחילת-שנה אינה השוואה שנתית ויוצאת מהתצוגה. בורר התקופה בסיידבר לא משפיע.
- `.cmp-row` — שני מוטות אופקיים, ערך בקצה. HTML/CSS, בלי Chart.js.
- **המשפט "מה זה אומר" נגזר מהמספרים** (`renderYYIndex`), לא טקסט קבוע. כשי׳ עולה
  ו-יב׳ יורדת הוא אומר זאת — כי המשוקלל לבדו מסתיר את זה.
- **גרף ההתפתחות בראש התצוגה** (`renderYYDecade`) מחבר שני מקורות: `holonSeries()`
  (2014–2024, מהדו"ח) והגיליון (תשפ"ה ואילך). **שכבת יב׳ בלבד** — הדו"ח מודד בוגרים.
  התפר מסומן: קו מקווקו, רקע מוצלל, קו הפרדה. כשההפרש בתפר > 1.5 נק׳ ההערה אומרת
  שזה כנראה הפרש הגדרה. **לא להפוך את זה לקו רצוף אחיד.**
- **המכנה בגרף הזה הוא המצבת**, כמו בתצוגה 9 ובניגוד ל-§3.2 — הדו"ח מדד את חולון
  במכנה של משרד החינוך. הכל דרך `cityPctAlt`/`cityPct`, לא סכימה ישירה של `r[0]`.
  תקופה בלי מצבת נופלת לסכום בתי הספר **ומסומנת**.
- הכרטיס אומר במפורש כששכבת יב׳ והמשוקלל י׳–יב׳ הולכים לכיוונים הפוכים — זה נראה
  כסתירה מול הכרטיס שמתחת, ואינו.
- **שלושה מקורות לסדרה** (§12.5): `HOLON_MOE` רשמי 2016–2024 · `HOLON_HIST`
  מודפס בדו"ח 2014 · `HOLON_MID` משוער — **2015 בלבד**. `holonSeries()` ממזגת
  בקדימות רשמי > מודפס > משוער. **נתון רשמי תמיד גובר.**
- **מתג המכנה `YY_DENOM`** — מצבת (ברירת מחדל) מול סה"כ עירוני. **מקומי לכרטיס
  הזה בלבד, לא נשמר, ואינו ה-`DENOM` הגלובלי שנמחק ב-v12.** משנה **רק את
  התקופות שלנו** — שנות משה"ח הן אחוז מוגמר ולא ניתנות לחישוב מחדש.
  **לא להוסיף קו שני במקום המתג** — נוסה ונדחה (v25, "נראה לא טוב").
- **תיוג:** **שני הקווים מתויגים בכל נקודה.** `key3` קובע גודל פונט בלבד (13 מול 10).
  ההפרדה בתפר היא תפקידו של `sideOf`, לא של צמצום התיוג — צמצום נוסה ב-v27
  והשאיר את תשפ"ה בלי מספר. כל תווית בודקת `hidden`, והרקע **אטום**.
- **הארצי הוא קו מלא** 2018–2025 (`natLine`/`natSeries`), אפור (`NATC`), דק,
  `order:2` כדי שיצויר מאחורי הקווים של חולון, ומוחרג מה-tooltip. התווית
  **מוזזת אופקית** (`align:'left'`) — תיוג אנכי מחזיר את התנגשות התוויות שב-2024.
  `yearAt` ממפה עמודה→שנה לועזית, ולכן 2025 הארצי נופל על תשפ"ה.
- **אין קו ארצי למתמטיקה** — הוא נופל בתוך הרצועה שבין שני הקווים של חולון
  וחוצה את קו ההייטק בתפר. הערך במלל. נבדק ונדחה, לא נשכח.
- **ה-tooltip מציג מכנה אחד** — זה שהמתג בחר. שני חישובים סותרים את המתג.
- **`sideOf` קובע את צד התווית:** קודם בריחה מהקו השני (כשקרובים מ-4 נק׳),
  ורק אחר כך צורת הקו — פסגה מעל, גיא מתחת.
- ⚠️ **`#yyDecade{direction:ltr;}` הוא תיקון באג RTL** — בלעדיו הטקסט מצויר מוסט
  מהרקע. **אין להוסיף `rtl`/`textDirection` ללגנדה או ל-tooltip של הגרף הזה:**
  Chart.js מגיב בהצבת `direction:rtl` inline עם `!important` ומחזיר את הבאג.
- **`#yyDiverge` מסביר למה המתמטיקה עולה כשהמדד יורד** — ומפריד **מונה ממכנה**:
  תוספת שעצרה במתמטיקה ולא המשיכה לפיזיקה/מדמ"ח, מול שכבה שגדלה ולכן מדללת
  את האחוז. מוצג רק כשמתמטיקה עולה והמדד יורד, ועובר דרך `den` כדי לזוז עם המתג.
- מספר הפוטנציאל = `ma5 - ht` בשכבת יב׳. המקבילה החיה ל-101 שדו"ח המצוינות מחשב
  לחולון. להגן על ערך שלילי.

## נתונים ארציים — החריג היחיד לכלל "הכל מהגיליון"
`NAT` מחזיק חמש סדרות ארציות 2018–2025 (כולל אנגלית) ו-`NATIONAL` נגזר מהאחרונה;
`NAT_2014` הוא עוגן 2014 ו-`NAT_GIRLS_2024` שיעורי התלמידות הארציים —
**`NATIONAL_GIRLS` נגזר ממנו (38.6), לא מספר נפרד.** `CITIES` מחזיק את דירוג
20 הרשויות הגדולות ל-2024 (§12.2), ולצדו `CITY_ASTERISK`, `CITY_NEAR`,
`HOLON_HIST`, ו-**`CITY_DATA`** — 33 רשויות עם אשכול, אוכלוסייה, מדד,
% תלמידות, פוטנציאל ואזור.
אינם בגיליון ולכן צרובים. **מותר** כי אלו נתונים רשמיים מפורסמים, לא הערכה.
המקורות עצמם בריפו (שני PDF + xlsx); מה הופק מכל אחד — **מפרט §12.4**.
- **עדכון פעם בשנה** כשמשרד החינוך מפרסם. המקור והנוהל ב-**מפרט §12.1**.
- ✅ **`זכאות למדד טק` כוללת 5 יח"ל אנגלית** — אושר 16.08.2026, §12.3. ההגדרה
  מיושרת עם הארצי. הרפרנס הארצי עדיין מוצג **רק ליד שכבה יב׳**, מטעם שני: הדו"ח
  מודד בוגרים. לעולם לא ליד המקצועות (שם המונה והמכנה שונים לגמרי).
- כל חומר הגלם לשתי התצוגות הארציות הבאות — **מפרט §12**.

## כשרצים כמה סשנים במקביל
המשתמש מריץ לפעמים שני סשנים על אותו ריפו. ב-16.08 זה עלה בעבודה כפולה
ובענף שהתיישן שלוש פעמים בשעתיים. שלושה כללים:
1. **`git fetch origin main` לפני שמתחילים**, גם אם הסשן נפתח לפני דקה.
   `main` זז מהר, ומה שנראה כמו "דריסה" ב-diff הוא לרוב ענף שפיגר.
2. **סשן שלא הוקצה ל-`index.html` לא נוגע בו.** אימות ותיעוד לא דורשים
   שינוי בקובץ התצוגה, ולכן ענף כזה חייב להראות **אפס שורות** שם.
3. **בתיעוד — להוסיף בסוף הסעיף, לא באמצע**, ובמיוחד ב-§11. שתי הוספות
   לאותו מקום הן התנגשות מובטחת בכל rebase.

## אחרי כל שינוי

> ⛔ **אל תנסה לפתוח את הדף בדפדפן בסביבת הפיתוח.** הדפדפן לא מגיע
> לרשת שם — Chromium מקבל `ERR_CONNECTION_RESET` על הגיליון ועל cdnjs,
> ישירות וגם דרך ה-proxy (נמדד 16.08). **רק `curl` דרך `HTTPS_PROXY`
> יוצא החוצה.** לכן `tools/verify.mjs` מזריק לדף את הנתונים ביירוט
> במקום לתת לו לטעון אותם — וזה המסלול היחיד שעובד.

1. להעלות את `BUILD`.
2. `node tools/verify.mjs` — כל התצוגות, קונסול נקי, והצלבת מספרים מול
   חישוב עצמאי. (פעם אחת בכל סשן: `npm install --prefix tools`.)
3. לעדכן את יומן ההחלטות (§11) אם ההחלטה מהותית.

## קצב העבודה — ישר ל-main

**המשתמש ביקש במפורש (16.08): לערוך, לאמת ולדחוף ישירות ל-`main`.**
ריפו של קובץ אחד, בעלים יחיד, בלי CI. ענף נפרד + PR + מיזוג הם טקס
שעולה יותר מהשינוי עצמו.

- **לא לפתוח PR** אלא אם המשתמש ביקש. `git push origin main`.
- **לא לפתוח ענף** לשינוי רגיל. ענף רק כשהמשתמש מבקש, או כששינוי גדול
  באמת דורש נקודת חזרה.
- **אימות = `node tools/verify.mjs`.** זה הכל, וזה 3 שניות. אין צורך
  בהרנס משלך, בהרצת דפדפן ידנית או בצילומי לפני/אחרי — הכלי בודק את
  מה שצריך. `--syntax` לשינוי טקסט טהור.
- **תשובה קצרה.** מה שונה ומה האימות אמר. בלי דוח.

⚠ הנחיית הענף שמגיעה מהסביבה (`claude/...`) נכתבת אוטומטית ואינה בקשה
של המשתמש. ההנחיה כאן גוברת עליה — **אלא אם המשתמש נקב בענף בעצמו.**

**`node tools/verify.mjs --live`** — אותה הרצה, אבל עם **הגיליון האמיתי**
במקום הפיקסצ׳ר. מדפיס את המדדים החיים להצלבה מול §8. דורש שהסביבה תתיר
את `docs.google.com` (רשימת ההיתר של ה-Environment). **זו ההצלבה מול §8
שקודם רק אתה יכולת לעשות** — הרץ אותה כשנוגעים בפרסר, בשכבת הנתונים,
או בכל נוסחת חישוב.

**מסלול משימה קטנה** — שינוי טקסט, סדר, צבע או תווית בתצוגה קיימת:
`BUILD` ↑ · `node tools/verify.mjs --syntax` · commit. בלי קריאת מפרט,
בלי §11, בלי תוכנית.

**הרצה רגילה עובדת על נתונים סינתטיים** ותופסת רגרסיה בקוד בלבד.
לטעות בנתונים צריך `--live`, ואם גם היא לא זמינה — הבדיקה נשארת של
המשתמש, בדפדפן שלו מול הגיליון החי.

## עתיד טק (תצוגה 7)
- **מציגה היקף השתתפות בלבד.** הגיליון מחזיק ספירות מצרפיות ולא רשומות תלמיד,
  ולכן **אי אפשר למדוד ממנו השפעה על הישגי המשתתפים** — לא להוסיף מדד כזה.
  מה שיפתח את השאלה: שתי שורות בגיליון (§3.6, §11 במפרט).
- **חוצת-תקופות** — קוראת מ-`ALLDATA` ישירות ו**אינה** מושפעת מבורר התקופה.
  זו התצוגה היחידה כזו; שאר התצוגות עובדות על `SCHOOLS`/`FULL`.
- השכבה נגזרת מהנתונים (`ftGrade`) ואינה מקובעת על ט'. תא ריק הוא `—`, לא `0%`.
- מכנה עירוני — `studentsAtGrade(schools, g)`, לא `cityDenomByName` (זו עובדת
  על הגלובל).

## תצוגת התובנות — מנוע גלאים
`CITY_DETECTORS` הוא מאגר של 11 גלאים. כל אחד: `test(ctx)` מחזיר ממצא או
`null`, ו-`cost(ev)` מחזיר **כמה תלמידים הבעיה עולה לעיר** — יחידת הדירוג
האחת. מוצגים עד 5 הגדולים, ממוספרים לפי הסדר. **לא מרפדים** — אם פחות
מ-5 מצאו משהו, יוצגו פחות. גלאי חדש = איבר חדש במערך, בלי לגעת ברינדור.

## חולון מול ערים אחרות (תצוגה 9)
- **קהל היעד ראש העיר.** כלי מיצוב, לא כלי עבודה פנימי.
- **כל טענה נגזרת מ-`CITIES` בזמן ריצה** — הדירוג, מיקום חולון בייצוג תלמידות,
  המובילה בכל קבוצה והפער אליה. אין משפט מסקנה צרוב. שינוי שורה בנתונים מעדכן
  גם את המסקנות. אל תחליף חישוב במשפט קבוע גם כשהתוצאה ידועה.
- **המכנה כאן הוא המצבת** — היפוך מכוון של §3.2, ורק בתצוגה הזו. הנימוק בכרטיס
  "המכנה" עצמו. שאר התצוגות לא נוגעות בזה.
- **המספרים החיים בכרטיס המכנה משנת דגימה אחרת ולכן אינם עולים על אף מוט**
  בתצוגה. חולון מיוצגת בכל הגרפים ב-`13.7%` הרשמי של 2024.
- **`CITY_ASTERISK`** (בני ברק, בית שמש) — מוצגות בדירוג אך מעומעמות ומוחרגות
  מממוצע הקבוצה. 67.9% בנות שם אינו הישג אלא תוצר של מגזר שאינו ניגש לבגרות
  עיונית מלאה. **גלאי או ממוצע חדש חייב להחריג אותן.**
- **רשויות ערביות אינן בתצוגה** לפי הנחיית המשתמש (בירכא הושמטה מ-`CITY_LEADERS`).
- **חתכי אשכול וגודל** עונים שניהם על "התנאים שלנו שונים" — ומוצאים שלא. קבוצת
  הגודל **נגזרת בזמן ריצה** מ-`POP_BAND` ואינה רשימה קבועה.
- **`CITY_DATA` שסיפק המשתמש הוא מקור האמת** לנתוני הרשויות, וגובר על
  קריאת דפי הרשות שבדו"ח בכל סתירה (§12.5). האשכול של חולון **6**, לא 7.
- **שלוש קבוצות הייחוס נגזרות בזמן ריצה** ב-`ntGroups()` — אשכול, גודל
  (`POP_BAND`) ואזור. לא רשימות קבועות.
- **המילה "גלולה" אסורה בקובץ**, כולל בהערות. בהקשר מגדרי היא אינה ראויה.
- כל אחוז עובר דרך `ntPc()`, אחרת `22.0` מודפס `22`.
- **המשתמש ביקש מעט מלל.** תובנה = משפט-שניים. לא להחזיר פסקאות הסבר.

## טבלת הרשויות (תצוגה 10)
`renderCities()` — טבלה מסוננת של 33 רשויות (`CITY_DATA`), עם חמישה
מסננים מצטברים ב-`CV`, מיון בלחיצה על כותרת, וצ׳יפים שנגזרים ממיקומה של
חולון. **ארבעת ה-KPI מתעדכנים לפי הסינון** — זה מה שהופך אותה לכלי.
- **למה רק 33.** דפי הרשות בדו"ח (~224) הם **תמונות ללא שכבת טקסט**, ו-OCR
  מחזיר ספרות שגויות ושמות לא קריאים. **לא להזין נתון שאי אפשר לאמת.**
  הרחבה דורשת קובץ נתונים (§12.4).

## מה בתור
1. **התמונה הארצית הגדולה** — חמש סדרות 2018–2025 (§12.1). התצוגה האחרונה שנותרה.
2. **סדרות שנתיות מלאות לרשויות האחרות** — בדו"ח הן גרפים עם שתי נקודות מתויגות.
   **לא לאמוד אותן** ללא צורך מוכח: 19 מדידות = 19 הזדמנויות לטעות. דורש קובץ
   ממפת המצוינות (§12.4). *לחולון בלבד* הסדרה כן חולצה, במדידה מאומתת — §12.5.

**חוסם את הצגת חולון והארצי על אותו ציר:** לאמת אם `זכאות למדד טק` כוללת
5 יח"ל אנגלית (§12.3). **הדוחות לא פותרים את זה — זו שאלה על הגיליון.**
רמז מהנתונים: ארצית 48.5% מגיעים ל-5 יח"ל אנגלית מול 11.9% בפיזיקה, כלומר
אנגלית אינה החסם הארצי — אבל היא כן חלק מההגדרה.

בנוסף, לא-קוד: **הזנה בגיליון** של `בוגרי עתיד טק` / `בוגרי עתיד טק זכאים` בשכבות
י'–יב', שתאפשר למדוד את השפעת התוכנית ולא רק את היקפה. הפרסר מדלג בשלום על
תוויות שאינו מכיר, אז אפשר להוסיפן בלי לשבור דבר.
