# דשבורד מדד בגרות-טק — חולון

## מה זה
קובץ HTML **יחיד** (`index.html`) שמתארח ב-GitHub Pages ומושך נתונים חיים
מגיליון גוגל מפורסם. אין DB, אין קבצי נתונים, אין שלב בנייה.

חי בכתובת: https://ramishaked.github.io/HolonTechDeshboard/

## לפני שנוגעים בקוד
`holon_live_spec.md` מכיל את כל ההחלטות שסוכמו — מבנה הגיליון, חוקי החישוב,
השפה העיצובית, ומה אסור לשבור. **אין צורך לדון מחדש במה שכתוב שם.**

**קרא את האינדקס שבראשו, ואז את הסעיפים הנוגעים למשימה** — לא את כולו.
המסמך הוא ~25K טוקנים; קריאה מלאה בכל משימה היא מס פתיחה כבד ומיותר.

**קריאה מלאה נדרשת** רק בשלושה מקרים: שינוי חוק חישוב (§3), הוספת תצוגה
חדשה, או נגיעה בשכבת הנתונים (הפרסר, `boot`, `GID_MAP`).

## כללי ברזל
1. **לא לשבור את העיצוב הקיים.** צבעים, פריסה, sidebar, גדלי פונט, מבנה
   הכרטיסים וסוגי הגרפים — אושרו והמשתמש התרגל אליהם. שינוי רק באישור מפורש.
2. **לא להמציא נתונים.** הכל מהגיליון, בזמן ריצה.
3. **קובץ אחד — באפליקציה.** `index.html` נשאר יחיד: לא לפצל ל-JS/CSS
   נפרדים, לא להוסיף שלב בנייה, ולא ליצור לו תלות בשום קובץ אחר בריפו.
   (`tools/` הוא אימות מקומי בלבד. GitHub Pages לא מגיש אותו, והדף לא
   יודע שהוא קיים.)
4. **חישוב עצמאי.** שורות `סה"כ עיר` שבגיליון אינן מקור — רק כלי הצלבה.
5. `זכאות למדד טק` הוא המדד. לא לסכום פיזיקה+מדמ"ח (יש חפיפה).

## הסיידבר (v42 · v45)
**הלוגו של מינהל החינוך מוטמע כ-data URI** (440px, 256 צבעים, ~17KB) ולא
כקובץ — הדף לא תלוי בשום נכס. הוא כהה ולכן יושב על לוח לבן; על רקע כהה
הוא נעלם. **אין להחליף אותו בקובץ חיצוני** גם אם זה נראה נקי יותר.
- **אייקוני התצוגות הם SVG inline** עם `stroke="currentColor"`. לא אימוג׳י:
  במצב מכווץ הם כל מה שנשאר, ואימוג׳י נראה שם כצעצוע.
- **התפריט מתכווץ ל-64px** (`.sidebar.collapsed`, נשמר ב-`localStorage`).
  כפתור החץ הוא הדרך היחידה לפתוח בחזרה ולכן **חייב להישאר גלוי** בכל מצב.
  במצב מכווץ הקבוצות נפתחות בכפייה — אחרת אייקונים נעלמים בלי רמז.
- הצבע `--sidebar` הוא כחול-פחם `#16213a` (v45). סגול וכתום נשארו צבעי
  נתונים, ולא צבע רקע.

## תפריט הסיידבר (v42)
שלוש קבוצות אקורדיאון — **תמונה עירונית** · **פילוחים** · **מסקנות** — עם
שמות תצוגה קצרים שאינם זהים לכותרות העמודים. מצב הקבוצות ב-`localStorage`.
- **`deepdive` ו-`cities` אינן בתפריט בכוונה.** הן תצוגות המשך: נפתחות מתוך
  עמוד ההורה (כרטיס בית ספר · הפיל "טבלה מלאה"). `PARENT_OF` ממפה ילד→הורה
  ו-`switchView` מדליק את פריט ההורה. **מחיקת הכניסה שבעמוד ההורה מפילה את
  `verify.mjs`** — הוא מגיע אליהן דרך הרכיב האמיתי ולא ב-`switchView`.
- **קבוצה סגורה לא נפתחת בכוח** כשהתצוגה הפעילה בתוכה — נקודה כתומה בלבד.
- לא להחזיר את `.sb-foot` ואת כפתור "מקור הנתונים" המלולי. גלגל השיניים
  שליד "תקופת דגימה" הוא הכניסה היחידה לחלון המקור.

## מבנה הקוד ב-index.html
| אזור | תוכן |
|------|------|
| `<style>` | כל ה-CSS. משתני הצבע ב-`:root` |
| HTML | sidebar, `section.view` לכל תצוגה (המספר נגזר ב-`verify --map`), חלון "מקור הנתונים" |
| `<script>` | מלכודת שגיאות → helpers → 10 פונקציות `render*` → שכבת הנתונים |

**שכבת הנתונים** בסוף הסקריפט: `GID_MAP` → `csvParse` → `parseSheet` →
`boot()` → `applyPeriod()`. `BUILD` הוא מספר הגרסה שמוצג בסיידבר —
**להעלות אותו בכל שינוי**, אחרת אי אפשר לדעת איזו גרסה רצה בדפדפן.

### להתמצא בקובץ — grep במקום סריקה
הקובץ הוא ~4,500 שורות. **לחפש לפי שם פונקציה, לא לקרוא רצף.**

```
node tools/verify.mjs --map
```
מדפיס את כל הפונקציות שבקובץ, מקובצות לפי תחום (רינדור · תצוגה 8 · ערים
אחרות · עתיד טק · מדד ומכנה · בית ספר · טעינה · ניווט), עם `BUILD` ומספר
השורות. **נגזר מ-`index.html` בזמן ריצה.**

מפה כתובה ביד עמדה כאן קודם ו**התיישנה תוך שעה** — תצוגה נוספה ושלוש
פונקציות `nt*` הוסרו באותו יום. עם כמה סשנים שעובדים במקביל היא תהיה
שגויה רוב הזמן, ומפה שגויה גרועה מאין מפה. **לא להחזיר אותה.**

## מבנה הנתונים
```js
SCHOOLS = [{ name, note, partial, grades: { 'י': [...15 שדות] } }]
// 0 total  1 cs  2 ph  3 ma5  4 ht  5 cs_g  6 ph_g  7 ht_g
// 8 amat   9 ma5_g  10 ft  11 ft_g  12 ma3  13 ma4  14 en5
FULL = SCHOOLS.filter(s => !s.partial)   // דירוג ונשירה בלבד
```
- **"מגמה י׳→יב׳" מחושבת תמיד על אחוזים**, לא על מספרים — ה-KPI העירוני,
  `schoolDropPct` ותצוגת המקצועות. על מספרים היא סופגת גם את הפרש גודלי
  השכבות, וזו ההטיה שכרטיס "אפקט המשפך" קיים כדי להפריד.
- **מדדים עירוניים** → `SCHOOLS` (כל בית ספר בכל שכבה שיש לו נתונים)
- **דירוג בין בתי ספר + נשירה** → `FULL` (דורש י'–יב' מלאים)
- **כל מכנה עירוני עובר דרך `cityDenomByName(שכבה)`** — סכום שורות
  `כמות תלמידים`. מכנה עירוני חדש חייב לקרוא לה ולא לסכום `total` בעצמו.
- **המצבת אינה מכנה** — היא מספר ייחוס שמוצג בסוגריים ליד המדד
  (`cityDenomAlt` → `cityPctAlt` → `altSpan`, §3.2). אין מתג. המונה לא מושפע.
  **החריג היחיד: תצוגה 9** (חולון מול ערים אחרות), שם המצבת היא המכנה הראשי כי
  זה המכנה שכל שאר הרשויות נמדדו בו. ההיפוך מקומי לאותו כרטיס בלבד.
- `amat=1` — בחט"ב פיזיקה=מדמ"ח=זכאות, כלומר רישום קבוצת עמ"ט ולא בחירת
  מקצוע. מוצג כקו אחד, לא שלושה.

## גישה לתקופה שאינה הנבחרת (תצוגה 8)
`applyPeriod` מציב ב-`SCHOOLS`/`FULL`/`MATZEVET` את התקופה הנבחרת בלבד.
כל התקופות כבר בזיכרון ב-`ALLDATA` (נטענות במקביל בכל `boot`).

**לחשב על תקופה אחרת = `withPeriod(key, fn, filter?)` בלבד.** היא מחליפה את
הגלובלים זמנית, מריצה פונקציית חישוב **קיימת**, ומשחזרת ב-`finally`:
```js
const p10_2025 = withPeriod('תשפה-2', () => cityPct(0));
```
- **לא לכתוב גרסה מקבילה של נוסחה** שמקבלת תקופה כפרמטר. זו הסיבה שהמספרים
  בהשוואה השנתית זהים בהגדרה לאלה שבסקירה העירונית.
- **בתוך `fn` רק חישוב טהור.** לא `render*`, לא DOM, לא `new Chart` —
  דליפה של הגלובלים משבשת בשקט את כל התצוגות האחרות.
- `filter` (רשות) מצמצם את בתי הספר לתת-קבוצה.
- סדר כרונולוגי: `periodsSorted()`.
- **תצוגה 7 (עתיד טק) עושה זאת אחרת** — מעבירה `schools` כפרמטר
  (`ftCity(schools, g)`, `studentsAtGrade(schools, g)`). זו הדרך הנכונה לחישוב
  חדש וקצר; `withPeriod` הוא לכשצריך **בדיוק** את נוסחאות התצוגות הקיימות.

## השוואה שנתית (תצוגה 8 — "התפתחות המדד" בתפריט)
- **מספור התצוגות ≠ סדר התפריט ≠ שם התצוגה.** "תצוגה 8" הוא מזהה לפי סדר בנייה;
  בסיידבר היא מופיעה בשם **"התפתחות המדד"**, שלישית בקבוצת "תמונה עירונית".
  לא לשנות מספור בעקבות שינוי בתפריט.
- **שאלה אחת: מה השתנה בין סוף השנה שעברה לסוף השנה הזו.** לא ציר זמן, לא מחזורים,
  לא מטריצה. הגרסה הקודמת (v16) ניסתה את כל אלה ונדחתה כמסובכת מדי להנהלה.
- `yyPeriods()` בוחרת את **שתי דגימות סוף-השנה האחרונות** (`half===2`). דגימת
  תחילת-שנה אינה השוואה שנתית ויוצאת מהתצוגה. בורר התקופה בסיידבר לא משפיע.
- `.cmp-row` — שני מוטות אופקיים, ערך בקצה. HTML/CSS, בלי Chart.js.
- **המשפט "מה זה אומר" נגזר מהמספרים** (`renderYYIndex`), לא טקסט קבוע. כשי׳ עולה
  ו-יב׳ יורדת הוא אומר זאת — כי המשוקלל לבדו מסתיר את זה.
- **גרף ההתפתחות בראש התצוגה** (`renderYYDecade`) מחבר שני מקורות: `holonSeries()`
  (2014–2024, מהדו"ח) והגיליון (תשפ"ה ואילך). **שכבת יב׳ בלבד** — הדו"ח מודד בוגרים.
  התפר מסומן: קו מקווקו, רקע מוצלל, קו הפרדה. כשההפרש בתפר > 1.5 נק׳ ההערה אומרת
  שזה כנראה הפרש הגדרה. **לא להפוך את זה לקו רצוף אחיד.**
- **המכנה בגרף הזה הוא המצבת**, כמו בתצוגה 9 ובניגוד ל-§3.2 — הדו"ח מדד את חולון
  במכנה של משרד החינוך. הכל דרך `cityPctAlt`/`cityPct`, לא סכימה ישירה של `r[0]`.
  תקופה בלי מצבת נופלת לסכום בתי הספר **ומסומנת**.
- הכרטיס אומר במפורש כששכבת יב׳ והמשוקלל י׳–יב׳ הולכים לכיוונים הפוכים — זה נראה
  כסתירה מול הכרטיס שמתחת, ואינו.
- **שלושה מקורות לסדרה** (§12.5): `HOLON_MOE` רשמי 2016–2024 · `HOLON_HIST`
  מודפס בדו"ח 2014 · `HOLON_MID` משוער — **2015 בלבד**. `holonSeries()` ממזגת
  בקדימות רשמי > מודפס > משוער. **נתון רשמי תמיד גובר.**
- **מתג המכנה `YY_DENOM`** — מצבת (ברירת מחדל) מול סה"כ עירוני. **מקומי לכרטיס
  הזה בלבד, לא נשמר, ואינו ה-`DENOM` הגלובלי שנמחק ב-v12.** משנה **רק את
  התקופות שלנו** — שנות משה"ח הן אחוז מוגמר ולא ניתנות לחישוב מחדש.
  **לא להוסיף קו שני במקום המתג** — נוסה ונדחה (v25, "נראה לא טוב").
- **תיוג:** **שני הקווים מתויגים בכל נקודה.** `key3` קובע גודל פונט בלבד (13 מול 10).
  ההפרדה בתפר היא תפקידו של `sideOf`, לא של צמצום התיוג — צמצום נוסה ב-v27
  והשאיר את תשפ"ה בלי מספר. כל תווית בודקת `hidden`, והרקע **אטום**.
- **הארצי הוא קו מלא** 2018–2025 (`natLine`/`natSeries`), אפור (`NATC`), דק,
  `order:2` כדי שיצויר מאחורי הקווים של חולון, ומוחרג מה-tooltip. התווית
  **מוזזת אופקית** (`align:'left'`) — תיוג אנכי מחזיר את התנגשות התוויות שב-2024.
  `yearAt` ממפה עמודה→שנה לועזית, ולכן 2025 הארצי נופל על תשפ"ה.
- **אין קו ארצי למתמטיקה** — הוא נופל בתוך הרצועה שבין שני הקווים של חולון
  וחוצה את קו ההייטק בתפר. הערך במלל. נבדק ונדחה, לא נשכח.
- **ה-tooltip מציג מכנה אחד** — זה שהמתג בחר. שני חישובים סותרים את המתג.
- **`sideOf` קובע את צד התווית:** קודם בריחה מהקו השני (כשקרובים מ-4 נק׳),
  ורק אחר כך צורת הקו — פסגה מעל, גיא מתחת.
- ⚠️ **`#yyDecade{direction:ltr;}` הוא תיקון באג RTL** — בלעדיו הטקסט מצויר מוסט
  מהרקע. **אין להוסיף `rtl`/`textDirection` ללגנדה או ל-tooltip של הגרף הזה:**
  Chart.js מגיב בהצבת `direction:rtl` inline עם `!important` ומחזיר את הבאג.
- **`#yyDiverge` מסביר למה המתמטיקה עולה כשהמדד יורד** — ומפריד **מונה ממכנה**:
  תוספת שעצרה במתמטיקה ולא המשיכה לפיזיקה/מדמ"ח, מול שכבה שגדלה ולכן מדללת
  את האחוז. מוצג רק כשמתמטיקה עולה והמדד יורד, ועובר דרך `den` כדי לזוז עם המתג.
- **המילה "משפך" שמורה לכרטיס הזה** — הכרטיס בתצוגת המקצועות הוא "מ-י׳ ל-יב׳
  לפי מקצוע", והוא חתך בין שכבות באותה שנה ולא משפך של דרישות.
- **"אפקט המשפך" הוא ההמחשה של `#yyDiverge`** — לכל תקופה **שני מוטות על סקאלה
  משותפת**: שכבת יב׳ (אפור = המכנה) ולומדי מתמטיקה 5, חתוך ל"המשיכו" (טורקיז =
  מונה המדד) ול"עצרו במתמטיקה" (זהוב). **האחוז הוא ממש היחס טורקיז/אפור**, ולכן
  שני הגורמים — מונה ומכנה — נראים באותה תמונה. טורקיז וזהוב הם צבעי הקווים
  בגרף שמעל. HTML/CSS, לא Chart.js. **מעט מלל** — המספרים על המסך.
- מספר הפוטנציאל = `ma5 - ht` בשכבת יב׳. המקבילה החיה ל-101 שדו"ח המצוינות מחשב
  לחולון. להגן על ערך שלילי.

## נתון גולמי — `data/national.json` לפני הכל
כל נתון ארצי/רשותי שאינו מהגיליון יושב גם בקובץ קריא-מכונה, עם **מקור ושנה
לכל שדה**. הדף **אינו** קורא אותו (כלל "קובץ אחד"), אבל `verify.mjs` משווה
12 קבוצות ערכים ונכשל על פער. **לפני שנוגעים בנתון ארצי — לקרוא שם את
`עדיפות_ושנים`.**

⚠ **שתי שנים, ואסור לערבב:**
- **2025 — מצגת משה"ח (`מדד טק 2025.pdf`, מאי 2026), וכתבת ynet שמבוססת עליה.**
  זהו **הנתון העדכני, וזה מה שהתצוגות מציגות**: `NAT` ו-`NATIONAL` (11.6%).
- **2024 — דו"ח המצוינות.** משמש **רק** היכן שאין תחליף 2025: שיעור התלמידות
  (`NAT_GIRLS_2024`, אין פילוח מגדרי במצגת) ו-33 הרשויות (`CITY_DATA` — המצגת
  מביאה 20 מובילות בלבד, וחולון אינה בהן).

🔴 **פיזיקה ומדמ"ח — אל תחליף ביניהן.** הן הוחלפו פעם אחת (v48) על סמך ציטוט
עיתונאי והוחזרו (v49): **שקף 5 במצגת מסמן במקרא פיזיקה=צהוב 11.9%,
מדמ"ח=סגול 8.9%.** מקור ראשוני שיושב בריפו גובר על ציטוט ממנו. §12.6.

## נתונים ארציים — החריג היחיד לכלל "הכל מהגיליון"
`NAT` מחזיק חמש סדרות ארציות 2018–2025 (כולל אנגלית) ו-`NATIONAL` נגזר מהאחרונה;
`NAT_2014` הוא עוגן 2014 ו-`NAT_GIRLS_2024` שיעורי התלמידות הארציים —
**`NATIONAL_GIRLS` נגזר ממנו (38.6), לא מספר נפרד.** `CITIES` מחזיק את דירוג
20 הרשויות הגדולות ל-2024 (§12.2), ולצדו `CITY_ASTERISK`, `CITY_NEAR`,
`HOLON_HIST`, ו-**`CITY_DATA`** — 33 רשויות עם אשכול, אוכלוסייה, מדד,
% תלמידות, פוטנציאל ואזור.
אינם בגיליון ולכן צרובים. **מותר** כי אלו נתונים רשמיים מפורסמים, לא הערכה.
המקורות עצמם בריפו (שני PDF + xlsx + `data/socio_sources.csv`); מה הופק מכל
אחד — **מפרט §12.4**.
- **עדכון פעם בשנה** כשמשרד החינוך מפרסם. המקור והנוהל ב-**מפרט §12.1**.
- ✅ **`זכאות למדד טק` כוללת 5 יח"ל אנגלית** — אושר 16.08.2026, §12.3. ההגדרה
  מיושרת עם הארצי. הרפרנס הארצי עדיין מוצג **רק ליד שכבה יב׳**, מטעם שני: הדו"ח
  מודד בוגרים. לעולם לא ליד המקצועות (שם המונה והמכנה שונים לגמרי).
- כל חומר הגלם לשתי התצוגות הארציות הבאות — **מפרט §12**.

## כשרצים כמה סשנים במקביל
המשתמש מריץ לפעמים שני סשנים על אותו ריפו. ב-16.08 זה עלה בעבודה כפולה
ובענף שהתיישן שלוש פעמים בשעתיים. שלושה כללים:
1. **`git fetch origin main` לפני שמתחילים**, גם אם הסשן נפתח לפני דקה.
   `main` זז מהר, ומה שנראה כמו "דריסה" ב-diff הוא לרוב ענף שפיגר.
2. **סשן שלא הוקצה ל-`index.html` לא נוגע בו.** אימות ותיעוד לא דורשים
   שינוי בקובץ התצוגה, ולכן ענף כזה חייב להראות **אפס שורות** שם.
3. **בתיעוד — להוסיף בסוף הסעיף, לא באמצע**, ובמיוחד ב-§11. שתי הוספות
   לאותו מקום הן התנגשות מובטחת בכל rebase.

## אחרי כל שינוי

> ⛔ **אל תנסה לפתוח את הדף בדפדפן בסביבת הפיתוח.** הדפדפן לא מגיע
> לרשת שם — Chromium מקבל `ERR_CONNECTION_RESET` על הגיליון ועל cdnjs,
> ישירות וגם דרך ה-proxy (נמדד 16.08). **רק `curl` דרך `HTTPS_PROXY`
> יוצא החוצה.** לכן `tools/verify.mjs` מזריק לדף את הנתונים ביירוט
> במקום לתת לו לטעון אותם — וזה המסלול היחיד שעובד.

1. להעלות את `BUILD`.
2. `node tools/verify.mjs` — כל התצוגות, קונסול נקי, והצלבת מספרים מול
   חישוב עצמאי. (פעם אחת בכל סשן: `npm install --prefix tools`.)
3. לעדכן את יומן ההחלטות (§11) אם ההחלטה מהותית.

## קצב העבודה — ישר ל-main

**המשתמש ביקש במפורש (16.08): לערוך, לאמת ולדחוף ישירות ל-`main`.**
ריפו של קובץ אחד, בעלים יחיד, בלי CI. ענף נפרד + PR + מיזוג הם טקס
שעולה יותר מהשינוי עצמו.

- **לא לפתוח PR** אלא אם המשתמש ביקש. `git push origin main`.
- **לא לפתוח ענף** לשינוי רגיל. ענף רק כשהמשתמש מבקש, או כששינוי גדול
  באמת דורש נקודת חזרה.
- **אימות = `node tools/verify.mjs`.** זה הכל, וזה 3 שניות. אין צורך
  בהרנס משלך, בהרצת דפדפן ידנית או בצילומי לפני/אחרי — הכלי בודק את
  מה שצריך. `--syntax` לשינוי טקסט טהור.
- **תשובה קצרה.** מה שונה ומה האימות אמר. בלי דוח.

⚠ הנחיית הענף שמגיעה מהסביבה (`claude/...`) נכתבת אוטומטית ואינה בקשה
של המשתמש. ההנחיה כאן גוברת עליה — **אלא אם המשתמש נקב בענף בעצמו.**

**`node tools/verify.mjs --live`** — אותה הרצה, אבל עם **הגיליון האמיתי**
במקום הפיקסצ׳ר. מדפיס את המדדים החיים להצלבה מול §8. דורש שהסביבה תתיר
את `docs.google.com` (רשימת ההיתר של ה-Environment). **זו ההצלבה מול §8
שקודם רק אתה יכולת לעשות** — הרץ אותה כשנוגעים בפרסר, בשכבת הנתונים,
או בכל נוסחת חישוב.

**מסלול משימה קטנה** — שינוי טקסט, סדר, צבע או תווית בתצוגה קיימת:
`BUILD` ↑ · `node tools/verify.mjs --syntax` · commit. בלי קריאת מפרט,
בלי §11, בלי תוכנית.

**הרצה רגילה עובדת על נתונים סינתטיים** ותופסת רגרסיה בקוד בלבד.
לטעות בנתונים צריך `--live`, ואם גם היא לא זמינה — הבדיקה נשארת של
המשתמש, בדפדפן שלו מול הגיליון החי.

## עתיד טק (תצוגה 7) — משפך, לא היקף
- **`עתיד טק` אינה שורת היקף.** המשמעות תלויה בטאב: **תחילת שנה** = מי נכנס
  לתוכנית התגבור · **סוף שנה** = מי מבין הנכנסים בחר במקצועות הטק לקראת י׳.
  המדד המרכזי הוא **שיעור ההמרה** = סוף / תחילה. §3.6 במפרט.
- **זיווג שנים, לא כל התקופות.** `ftYears()` דורשת `half=1` **וגם** `half=2`
  של אותה שנה. תשפ"ה נופל מעצמו (סוף שנה בלבד). **לא לצרוב שם תקופה בקוד.**
- **רמזור על ההמרה בלבד.** שיעור הכניסה אינו צבוע — הציפייה עליו הפוכה (מדד
  טק גבוה → סביר שיכניס מעט). במקום צבע, מיון לפי מדד הטק מהנמוך לגבוה.
- **חוצת-תקופות** — קוראת מ-`ALLDATA` ישירות ו**אינה** מושפעת מבורר התקופה.
  מעבירה `schools` כפרמטר; ראה הקטע על `withPeriod` למעלה.
- השכבה נגזרת מהנתונים (`ftGrade`) ואינה מקובעת על ט'.
- **`0` = "אין נתון", לא אפס** (`ftAt`) — הפרסר ממפה ריק ואפס לאותו ערך.
  בית ספר ז׳–ט׳ מקבל `—` במדד הטק; זה **אינו** `0.0%`.
- מכנה שיעור הכניסה — `studentsAtGrade(schools, g)`, לא `cityDenomByName`
  (זו עובדת על הגלובל).
- **אי אפשר למדוד מכאן השפעה על ההישגים** — אין רשומות תלמיד ואין קבוצת
  השוואה. לא להוסיף מדד כזה. מה שיפתח את השאלה: שתי שורות בגיליון (§3.6).

## תצוגת התובנות — מנוע גלאים
`CITY_DETECTORS` הוא מאגר של 11 גלאים. כל אחד: `test(ctx)` מחזיר ממצא או
`null`, ו-`cost(ev)` מחזיר **כמה תלמידים הבעיה עולה לעיר** — יחידת הדירוג
האחת. מוצגים עד 5 הגדולים, ממוספרים לפי הסדר. **לא מרפדים** — אם פחות
מ-5 מצאו משהו, יוצגו פחות. גלאי חדש = איבר חדש במערך, בלי לגעת ברינדור.

## חולון מול ערים אחרות (תצוגה 9)
- **קהל היעד ראש העיר.** כלי מיצוב, לא כלי עבודה פנימי.
- **כל טענה נגזרת מ-`CITIES` בזמן ריצה** — הדירוג, מיקום חולון בייצוג תלמידות,
  המובילה בכל קבוצה והפער אליה. אין משפט מסקנה צרוב. שינוי שורה בנתונים מעדכן
  גם את המסקנות. אל תחליף חישוב במשפט קבוע גם כשהתוצאה ידועה.
- **המכנה כאן הוא המצבת** — היפוך מכוון של §3.2, ורק בתצוגה הזו. הנימוק בכרטיס
  "המכנה" עצמו. שאר התצוגות לא נוגעות בזה.
- **המספרים החיים בכרטיס המכנה משנת דגימה אחרת ולכן אינם עולים על אף מוט**
  בתצוגה. חולון מיוצגת בכל הגרפים ב-`13.7%` הרשמי של 2024.
- **`CITY_ASTERISK`** (בני ברק, בית שמש) — מוצגות בדירוג אך מעומעמות ומוחרגות
  מממוצע הקבוצה. 67.9% בנות שם אינו הישג אלא תוצר של מגזר שאינו ניגש לבגרות
  עיונית מלאה. **גלאי או ממוצע חדש חייב להחריג אותן.**
- **רשויות ערביות אינן בתצוגה** לפי הנחיית המשתמש (בירכא הושמטה מ-`CITY_LEADERS`).
- **חתכי אשכול וגודל** נועדו שניהם לענות על "התנאים שלנו שונים". ⚠ **מ-v57 הם
  אינם מסכימים**: חתך הגודל מפריך את הטענה (חולון #3/9, מעל הממוצע), חתך
  האשכול **תומך בה** (#5/6, מתחת). לא לנסח כאילו שניהם עדיין מצביעים לאותו
  כיוון — המשפט באריח בתצוגה 11 נגזר (`refSay`). קבוצת הגודל **נגזרת בזמן
  ריצה** מ-`POP_BAND` ואינה רשימה קבועה.
- **`CITY_DATA` שסיפק המשתמש הוא מקור האמת** לנתוני הרשויות, וגובר על
  קריאת דפי הרשות שבדו"ח בכל סתירה — **חוץ מעמודת `socio`** (§12.4).
- 🔴 **האשכול של חולון הוא 7.** ה-6 נקבע ב-16.08 על סמך
  `israel_localities_socio_economic.xlsx` **ובוטל ב-17.08**: הצלבת שלושה
  מקורות (`data/socio_sources.csv`, 66 רשויות, עמודה F) מצאה שהקובץ הזה חורג
  ב-16 מתוך 66 ואינו קובע לאשכול — רק לאוכלוסייה. **עמודה F קובעת**, ותשע
  שורות תוקנו לפיה. אין לתייג את העמודה בשנת מדד יחידה — היא מעורבת.
- **שלוש קבוצות הייחוס נגזרות בזמן ריצה** ב-`ntGroups()` — אשכול, גודל
  (`POP_BAND`) ואזור. לא רשימות קבועות.
- **המילה "גלולה" אסורה בקובץ**, כולל בהערות. בהקשר מגדרי היא אינה ראויה.
- כל אחוז עובר דרך `ntPc()`, אחרת `22.0` מודפס `22`.
- **המשתמש ביקש מעט מלל.** תובנה = משפט-שניים. לא להחזיר פסקאות הסבר.

## תקציר מנהלים (תצוגה 11)
מסך אחד לראש העיר. **אפס נוסחאות חדשות ואפס גלאים חדשים** — הכל הרכבה
מפונקציות קיימות, ולכן זהה *בהגדרה* לשאר התצוגות. `execCtx()` מרכז את
המספרים. המכנה הוא **המצבת, שכבת יב׳** (היפוך של §3.2, כמו תצוגה 9);
טבלת בתי הספר חוזרת למכנה הפנימי ומתרגמת במפורש.
- **גריד KPI של 6 אריחים חשוף בראש (v68–v69 · v94)** — `execBottomLine`
  בונה `.ex-kpis` בשלוש עמודות: מספר + תת-שורה מספרית, **בלי משפטי
  מסקנה** (בקשת משתמש 18.08, "יותר מדי מלל באריחים"). המסקנות המילוליות
  חיות בבלוקים המתקפלים ובתצוגה 9 — לא להחזיר אותן לאריחים. חיצי
  `.ex-chip` נגזרים מהערך. האריחים: מדד רשמי+חץ · מול הארצי ·
  **ההתקדמות מתשפ״ד לתשפ״ה** (v78–v80, החליף את אריח היעד לתשפ״ט: טבלה
  זעירה, שורה למדד ולמתמטיקה 5, עמודה לכל שנה ועמודת שינוי, בלי תת־שורה,
  המכנה בסוגריים בכותרת; מהרשמי `HOLON_MOE` לדגימת הגיליון של השנה
  שאחריו דרך `prSheetPts`, מכנה המצבת; השנה נגזרת ולא צרובה) · מאגר
  הפוטנציאל · תלמידות · **המרת עתיד טק** (חוצת-תקופות). אריח בלי נתון
  **אינו מוצג**, לא מרופד. היעד עצמו נשאר בבלוק המתקפל.
  ⚠ **"שלושת המובילים" ו"הגיליון החי" הוסרו ב-v94** (בקשת משתמש 02.09) —
  לא להחזיר בלי בקשה. שלושת המובילים חיים בסקירה ובמפגש המנהלים,
  והגיליון החי בסקירה העירונית.
- **ששת הבלוקים האחרים אקורדיאון סגור** (v61), `execCard(id,title,lede,body)`
  בונה את כולם. ⚠ **הכותרת הסגורה נושאת מסקנה נגזרת** — שם משפט צרוב
  מסוכן פי כמה מאשר בגוף הבלוק, כי הוא כל מה שנקרא. `verify` תופס
  `.card-sub` ריק, לא ניסוח שקרי.
- **מילת כיוון לעולם אינה נכתבת — `dirWord`/`signed` גוזרים אותה.** זו
  קבוצת §9.1, והיא חזרה בקוד חדש תוך יממה. גם `girlsRank()` משותף.
- המצב ב-`holon_exec_open`, ונשמרים **הפתוחים** (ברירת מחדל = הכל סגור).
  `renderExec` בונה מחדש את כל `#exBody`, ולכן `open` מוזרק בבנייה
  וה-`toggle` נתפס בדלגציה ב-capture. הכפתור "פתח הכל" נדרש **להדפסה**.
- **סף היעד אינו צרוב** — `CITIES[RANK_TGT-1]`, נגזר בזמן ריצה.
- **מידע בלבד, בלי המלצות** (v56, בקשת המשתמש): מוצג `d.body(ev)` ולא
  `d.action(ev)`. **דירוג אינו המלצה** — הכרטיסים ממיינים מספרים ואינם
  כותבים משפטי ציווי, ואין להחזיר `action` לתצוגה הזו. **החריג היחיד:**
  עמודת "הצעד עכשיו" בכרטיס הפוטנציאל (בקשת משתמש מפורשת, 18.08).
- **יחידת הדירוג היא תלמידים וקשב ניהולי, לא כסף** — אין בריפו שום
  נתון תקציב/מורים/כיתות, ואין להמציא מדד עלות. שמות בתי הספר נגזרים.
- **`HOLON_POT_2024`** — פירוק הפוטנציאל מהדו"ח (101 מדע · 22 מתמטיקה ·
  2 אנגלית). **הדו"ח נותן את ההרכב, הגיליון את הגודל.** לסמן תמיד את
  שנת המקור — 2024 אינה שנת הגיליון.
- **שתי טבלאות בתי ספר, ובכוונה אינן מסכימות:** "מי תורם כמה" ממוינת
  לפי **החוסר ביב׳** (ההווה), וטבלת הפעולה שבכרטיס הפוטנציאל לפי
  **מאגר י׳** (הצופה קדימה). בית ספר יכול לקבל ✓ שם ולהיות מוקד כאן.
  לא לאחד ולא למיין מחדש — זה בדיוק ההסבר שהמשתמש ביקש ב-v66.
- ✅ **קבוצות 0 ו-0ב׳ ב-§9.1 סגורות** (v61), ו-v68 הסירה מהאריחים גם את
  `natSay`/`refSay`/"ההישג הבולט". **הפריטים הצרובים שנותרו פתוחים אינם
  בתצוגה הזו** — הם בתובנת המקצוע הנושר ובכותרת תצוגה 1 (§9.1, קבוצה א׳).
  ⚠ **לא לתעד כאן מספרי שורות** — הם מתיישנים בכל commit; לחפש בטקסט.
- **"היכן טמון הפוטנציאל — ומה עושים עכשיו" (`execPotential`, v64→v66)** —
  הכרטיס המאוחד שבלע את "איך מגיעים ליעד" (`execFocus`, נמחק ב-v66) ואת
  עמודת "מאגר י׳" של "מי תורם כמה". שני מאגרים: י׳ **בשל** (נוסחת
  מתמטיקה 5 פחות במדד) וט׳ **גיוס** (נוסחת גלאי המזינים,
  `grades['ט'][3]−[4]`), קלמפ 0 לכל בית ספר. מוטות עירוניים `.fnl-*` +
  **טבלת פעולה** לכל בתי הספר, **ממוינת לפי מאגר י׳** — זה מה שמיישב את
  "קציר ראשון כאן ותחתון שם". עמודת "יב׳ היום" דרך `execTgtIn` המשותפת
  עם `execSchools`. **עמודת "הצעד עכשיו" היא בקשת משתמש מפורשת (18.08)**
  — חריג ל"בלי המלצות" של v56, לכרטיס הזה בלבד; הפעלים נגזרים מהמבנה
  (מגמות יא׳ · רישום עמ"ט בי׳). ⚠ בט׳ "במסלול" = רישום עמ"ט, ההשוואה
  לפער היא היטל קדימה, ובדיקת האנגלית הנגזרת — שלושתם מוצהרים בכרטיס
  ואין למחוק את ההצהרות. שמות שנים נגזרים מ-`hebYearName`.
- **"מי תורם כמה" הוא מ-v66 צילום מצב יב׳ בלבד** — בלי עמודת מאגר,
  והערתו מפנה קדימה לכרטיס הפוטנציאל. לא להחזיר לו את העמודה.

## מפגש מנהלים (תצוגה 12, v70–v73)
`renderPrincipals()` — תצוגת הישגים לפורום המנהלים. **חיובי בלבד, בלי
ביקורת ובלי שמות בתי ספר** — למעט אקורדיאון "חמשת המובילים", סגור
בברירת מחדל ונפתח רק בהחלטה במהלך המפגש. אפס נוסחאות חדשות (כמו
תצוגה 11) — הכל דרך `execCtx`/`cityPct`/`girlsRank`/`prConv`/`ntGroups`/
`prSheetPts`→`yyGrade12`/`holonSeries`. עברה שלושה סבבי משוב באותו יום —
**ההחלטות למטה נבחנו מול המשתמש, לא לפתוח מחדש.**
- **חיובי ≠ מרופד:** לכל שעון וכרטיס תנאי הצגה נגזר; כשהתנאי לא מתקיים
  הרכיב לא נבנה. אין לנסח "יפה" מספר רע — זו קבוצת §9.1.
- **פריסה סימטרית 3+3 (v73):** שלושה שעונים — מטרה (כתום, רצועת מטרה
  **ירוקה** על הקשת: סף החמישייה הנגזר עד `PR_GOAL_HI`=20) · שכבת י׳
  (ירוק) · תלמידות (סגול, סקאלה 50%) — ושלושה כרטיסים: העלייה
  במתמטיקה · **#8 מתוך 20 הגדולות** (v74, לחיץ אל חולון–ארצי; החליף
  את קבוצת הגודל) · המרת עתיד טק. "מאגר הצמיחה" הוסר ב-v73
  לטובת הסימטריה — לא להחזיר בלי לפנות מקום.
- ⚠ **`PR_SHEET_MAX_YEAR`=2025 — חסימה מפורשת (v74, החלטת משתמש):**
  ההמשך הנדגם בגרף ובכרטיס המתמטיקה נעצר בתשפ"ה (23.4 במצבת); תשפ"ו
  יורד ל-22.5 ותשפ"ז ריק. הרחבת הטווח — עדכון ידני של הקבוע בלבד.
- **שעוני המד** (`prGauge`) הם חצי-דונאט Chart.js בתוך אריחי `.kpi` —
  סוג גרף שאושר לתצוגה הזו בלבד. **בלי מחוג** (נוסה ב-v71, הוסר ב-v72 —
  הסתיר את המספר); במקומו שנתות מספריות על הקשת, ותוויות הקצה מצוירות
  **מעל** קו הבסיס (מתחתיו הן נחתכות, v73). סקאלת שעוני המדד = ההישג
  הגבוה בארץ (נגזר, מעוגל לכפולת 8). המספר ב-`.pr-g-num` (HTML).
- **גרף "התפתחות המדדים"** (`prDecade`, מתחת לכרטיסים) — הרשמי עד
  2024, ו**שני קווי חולון ממשיכים** לדגימות הגיליון החי עד
  `PR_SHEET_MAX_YEAR` (מקווקו + מעוינים + הערת מקור; המשך המדד הוחזר
  ב-v75 אחרי שעדכון הנתונים הפך את תשפ"ה לעלייה גם במדד). הארצי רשמי
  בלבד. ציר ה-Y דרך `niceAxis`, לא מאפס. `direction:ltr` על
  `#prDecade` ו-`.pr-g-wrap` — אותו תיקון RTL של `#yyDecade`.
- **"המובילים" — שכבת יב׳ בלבד** (המשוקלל בלבל), בתבנית המדויקת של
  "בתי ספר מובילים" שבסקירה (`.bn-head`+`.bn-list`), רק מי שמעל
  הממוצע העירוני ועד 5.
- **אין דירוג במכנה הפנימי.** כרטיס קבוצת הגודל מציג את הדירוג הרשמי;
  המדידה הפנימית הוצגה כמשפט ייחוס והוסרה ב-v72 לבקשת "פחות מלל".

## טבלת הרשויות (תצוגה 10)
`renderCities()` — טבלה מסוננת של 33 רשויות (`CITY_DATA`), עם חמישה
מסננים מצטברים ב-`CV`, מיון בלחיצה על כותרת, וצ׳יפים שנגזרים ממיקומה של
חולון. **ארבעת ה-KPI מתעדכנים לפי הסינון** — זה מה שהופך אותה לכלי.
- **למה רק 33.** דפי הרשות בדו"ח (~224) הם **תמונות ללא שכבת טקסט**, ו-OCR
  מחזיר ספרות שגויות ושמות לא קריאים. **לא להזין נתון שאי אפשר לאמת.**
  הרחבה דורשת קובץ נתונים (§12.4).

## צפי מחזור (תצוגה 14, v89–v98)
`renderForecast()` בקבוצת "מסקנות". **לא קו מגמה ולא אקסטרפולציה** — תשע
נקודות רשמיות אינן בסיס לכך, ובמדד שקפץ 2.5 נק׳ בשנה קו כזה מתפרק. במקום
זאת **מחזור**: יא׳ של היום היא יב׳ של השנה הבאה, י׳ של היום היא יב׳ בעוד
שנתיים, ו**שיעור השמירה נמדד** מאותם מחזורים בשתי דגימות סוף-שנה.
- **הכרטיס הראשי "תחזית התפתחות מדד טק" מציג שני מוטות צפי בלבד (v98,
  החלטת משתמש 03.09):** בפתיחת שנת לימודים המחזור שנמדד כבר סיים, ולכן
  הוא אינו מוט אלא **משפט ייחוס** ב"איך חושבנו" ("המחזור שסיים ב… עמד
  על…"). המשפט הזה הוא תנאי היושרה של הכרטיס — **אין למחוק אותו.**
  גרסה עם מוטות עבר (תשפ"ד+תשפ"ה, v95) נוסתה ובוטלה באותו יום ("לא
  אהבתי"); לא להחזיר שנות עבר לגרף בלי בקשה מפורשת. `verify` נכשל אם
  יש יותר משני מוטות או אם משפט הייחוס חסר.
- **אפס נוסחאות חדשות למונה ולמכנה:** `cityTotal` דרך `withPeriod`,
  `cityDenomAlt`/`cityDenomByName` למכנה, `execCard` לאקורדיאון.
- **המכנה הוא מצבת השכבה של אותו מחזור** (י׳ של היום במצבת של י׳ היום) —
  זה המכנה שנוסע עם המחזור. נפילה לסכום בתי הספר, מסומנת.
- 🔴 **שיעור מעל 100% אינו מוכפל קדימה** (`fcRates`, `p10`/`p11`). הוא
  קיים ומוצג כפי שנמדד, אבל 105% בריבוע = 110% ומייצר צפי מנופח. **הקלמפ
  ל-1 הוא החלטה, לא באג** — `verify` נכשל אם הוא מוסר.
- **שיעור בית ספר עצמו רק כשהבסיס ≥ `FC_MIN_BASE`** (10), אחרת העירוני,
  מסומן ★. בסיס של 4 תלמידים אינו שיעור.
- **סכום עמודת הצפי ≠ הצפי העירוני, ובכוונה** — השיעור העירוני סופג מעבר
  בין בתי ספר ויציאה מהעיר. הכרטיס אומר זאת; לא לאחד.
- **טבלת בתי הספר באקורדיאון סגור** (בקשת משתמש). היא משתמשת ב-`execCard`
  ולכן במפתח `holon_exec_open`; `fcsch` אינו נשמר שם (`execSaveOpen` סורק
  `#exBody` בלבד) ולכן היא **תמיד** נפתחת סגורה. זו התנהגות רצויה.
- **המספרים בטבלה הם זכאים, לא אחוזים.** שכבה קטנה מזיזה אחוז הרבה.
- **כרטיס המקצועות (v92, `fcSubjects`/`fcSubjCard`)** בין הכרטיס הראשי
  לאקורדיאון: אותו חישוב לכל מקצוע דרך `citySubjectByGrade`, אותו קלמפ.
  משפט הדליפה/השמירה נגזר. אין לסכם פיזיקה+מדמ"ח (חפיפה), ומתמטיקה 5
  אינה המדד. מקצוע בלי נתונים אינו מוצג (אנגלית אינה מוזנת).
- **מתג המכנה `FC_DENOM` (v93)** כמו `YY_DENOM`: מקומי, לא נשמר, `.dseg`
  בכותרת הכרטיס הראשי. `fcDen(g)` מכריע לכל שכבה, וגם כרטיס המקצועות
  עובר דרכה. **על "סה״כ עירוני" סימן היעד ומשפט היעד יורדים** — היעד
  נמדד במצבת (תצוגה 9) ואינו בר-השוואה במכנה אחר. `verify` בודק זאת.
- הצפי מניח שהרשומים ישלימו גם 5 יח״ל אנגלית. מוצהר בכרטיס, אין למחוק.

## שאלה על הנתונים (תצוגה 13, v83)
שדה שאלה חופשית ותשובה של Claude. **בלי צ'אט, בלי היסטוריה, בלי
localStorage.** החריג: **שאלת המשך אחת** (v87), נשלחת עם השאלה והתשובה
הקודמות (`prior`), ואחריה השדה נסגר. החלטת משתמש 02.09: לא להרחיב לצ'אט. `renderAsk()` בונה דוגמאות נגזרות ומחבר את הכפתור;
`askSend()` שולח; `buildAskPayload()` אורז את המספרים.
- **החריג היחיד לכלל "קובץ אחד":** הדף פונה לכתובת חיצונית, `ASK_URL`
  (ליד `GID_MAP`). זה פרויקט Vercel נפרד שתיקיית השורש שלו היא **`ask/`**
  בריפו הזה (`ask/api/ask.js`, `ask/README.md`). המפתח `ANTHROPIC_API_KEY`
  חי רק במשתני הסביבה של Vercel. `ASK_URL` ריק = הכפתור מנוטרל, שום
  תצוגה אחרת לא מושפעת. הרשת נפתחת **רק בלחיצה**; `verify.mjs` עונה
  ל-`*.vercel.app` בתשובה קבועה ובודק את החבילה, לא את המודל.
- **המודל מנסח, לא מחשב.** החבילה נבנית **רק** מנוסחאות קיימות דרך
  `withPeriod` (`cityPct`/`cityPctAlt`, `yyGrade12`, `ftCityFunnel`,
  `execCtx`, `ntGroups`, הגלאים) ומהשורות הגולמיות של כל התקופות. ההגדרות
  (מכנה בתי ספר מול מצבת, חלקיים, `amat`, עתיד טק) יושבות ב-`SYSTEM`
  שבפונקציה, והוראה לצטט רק מהחבילה ולומר כשאין נתון. **נוסחה חדשה
  לא נכנסת לחבילה; היא נכנסת קודם לתצוגה שלה.**
- מפתחות ממוינים (`askJson`) כדי שהחבילה תהיה זהה בין שאלות: השירות
  שם עליה נקודת מטמון. שינוי בסדר או בעיגול מבטל את המטמון בשקט.
- מודל: `claude-opus-5` (החלטת משתמש 02.09), `effort: medium`,
  `fallbacks: "default"`. החלפה: `ASK_MODEL` ב-Vercel, בלי לגעת בקוד.
- **מגבלת הקצב בקוד היא per-מופע וחלקית.** הרשת האמיתית היא מגבלת
  ההוצאה בקונסול של Anthropic. אין לוג שאלות, ואין להוסיף בלי החלטה.

## מה בתור
1. **התמונה הארצית הגדולה** — חמש סדרות 2018–2025 (§12.1). התצוגה האחרונה שנותרה.
2. **סדרות שנתיות מלאות לרשויות האחרות** — בדו"ח הן גרפים עם שתי נקודות מתויגות.
   **לא לאמוד אותן** ללא צורך מוכח: 19 מדידות = 19 הזדמנויות לטעות. דורש קובץ
   ממפת המצוינות (§12.4). *לחולון בלבד* הסדרה כן חולצה, במדידה מאומתת — §12.5.

**חוסם את הצגת חולון והארצי על אותו ציר:** לאמת אם `זכאות למדד טק` כוללת
5 יח"ל אנגלית (§12.3). **הדוחות לא פותרים את זה — זו שאלה על הגיליון.**
רמז מהנתונים: ארצית 48.5% מגיעים ל-5 יח"ל אנגלית מול 11.9% בפיזיקה, כלומר
אנגלית אינה החסם הארצי — אבל היא כן חלק מההגדרה.

בנוסף, לא-קוד: **הזנה בגיליון** של `בוגרי עתיד טק` / `בוגרי עתיד טק זכאים` בשכבות
י'–יב', שתאפשר למדוד את השפעת התוכנית ולא רק את היקפה. הפרסר מדלג בשלום על
תוויות שאינו מכיר, אז אפשר להוסיפן בלי לשבור דבר.
