# מפרט טכני — דשבורד מדד טק חולון (גרסת נתונים חיים)

> מסמך הנחיות לפרויקט. מכיל את כל ההחלטות והמבנה שסוכמו. **אין צורך לדון בהם מחדש.**

---

## 0. עקרון-על: אל תשבור את מה שעובד

הדשבורד הקיים (`holon_dashboard.html`) **עבר בדיקה ואושר**. המשתמש התרגל אליו.

**מותר לשנות:** את שכבת הנתונים בלבד — במקום `SCHOOLS` צרוב בקוד, טעינה דינמית מ-CSV.

**אסור לשנות בלי אישור מפורש:** צבעים, פריסה, sidebar, גדלי פונט, מבנה הכרטיסים, סוגי הגרפים, נוסח הכותרות.

תוספות חדשות (תצוגה השוואתית, רמזור עתיד טק) יבנו **באותה שפה עיצובית** בדיוק.

---

## 1. ארכיטקטורה

```
Google Sheet (טאב לכל תקופת דגימה)
        ↓  File → Share → Publish to web → CSV
        ↓  fetch() בזמן ריצה + כפתור "רענן נתונים"
holon_dashboard.html  (מתארח ב-GitHub Pages, ריפו נפרד)
```

- **אין DB.** הגיליון הוא מקור האמת היחיד.
- **אין נתונים צרובים בקוד.** הקוד קבוע, הנתונים נטענים.
- העדכון בגיליון מתפרסם אוטומטית ל-CSV (cache של דקות ספורות). כדי לעקוף cache: להוסיף `&_=${Date.now()}` ל-URL.

### URL בסיס (פעיל ונבדק)
```
https://docs.google.com/spreadsheets/d/e/2PACX-1vTh1LxJwgfVoabbtv0fn85ODhbzF1Br0njIWMEkuUEpRaSfgCgE9L6zwmRS7I6w7er1edHk5D1JjcOw/pub?output=csv
```

⚠️ **נקודה פתוחה לפתרון בפיתוח:** ה-URL הזה מחזיר **טאב אחד בלבד** (ברירת המחדל). לטעינת שאר הטאבים נדרש `&gid=<GID>` לכל טאב. בתחילת הפיתוח: לאסוף את ה-GID של כל טאב ולהחזיק מיפוי `{תקופה: gid}` בראש הקוד. חלופה: `&single=true&gid=...`.

---

## 2. מבנה הגיליון

### טאבים = תקופות דגימה
פורמט קבוע ומובטח: `תשפה (2)`, `תשפו (1)`, `תשפו (2)`, `תשפז (1)`, `תשפז (2)`...

| רכיב | פענוח |
|------|-------|
| תשפה | 2025 |
| תשפו | 2026 |
| תשפז | 2027 |
| (1) | תחילת שנה |
| (2) | סוף שנה |

מיון כרונולוגי לציר הזמן: לפי שנה, ואז לפי מספר הדגימה.

### מבנה פנימי — שטוח, זהה בכל טאב

- **עמודה 0** — שם בית ספר (מופיע בכל שורה)
- **עמודה 1** — שם המקצוע/המדד
- **עמודות 2+** — 6 בלוקי שכבות, 6 עמודות כל אחד

```
base(grade_index) = 2 + grade_index * 6

ז'  → 2   |  ח'  → 8   |  ט'  → 14
י'  → 20  |  יא' → 26  |  יב' → 32

בתוך כל בלוק:
  base+0  בנים (מספר)
  base+1  בנות (מספר)
  base+2  סה"כ (מספר)     ← העיקרי
  base+3  % בנים
  base+4  % בנות
  base+5  % סה"כ
```

**אימות מהיר:** באורט, שורת `כמות תלמידים` — עמודה 4 = 205 (ז'), עמודה 22 = 278 (י'), עמודה 34 = 277 (יב').

### 9 שורות לכל בית ספר (עמודה 1)

| שורה | תפקיד |
|------|-------|
| `כמות תלמידים` | **המכנה** — סה"כ תלמידים בשכבה |
| `פיזיקה 5 יח׳` | מקצוע טק |
| `מדעי המחשב 5 יח׳` | מקצוע טק |
| `מתמטיקה 3יח׳` | פוטנציאל (רקע בלבד) |
| `מתמטיקה 4 יח׳` | פוטנציאל (רקע בלבד) |
| `מתמטיקה 5 יח׳` | פוטנציאל (רקע בלבד) |
| `אנגלית 5 יח׳` | פוטנציאל (רקע בלבד) |
| `עתיד טק` | קבוצת חיזוק — רמזור |
| `זכאות למדד טק` | **★ המדד המרכזי** — כל החישובים עליו |

⚠️ שמות השורות חייבים להיות זהים לחלוטין בין טאבים (המשתמש משכפל טאב, אז זה מובטח). **בכל זאת** — הפרסר ינרמל (trim + כיווץ רווחים) לפני השוואה, ליתר ביטחון.

### שורות שאינן בתי ספר (בסוף הגיליון)

| מזהה בעמודה 0 | טיפול |
|----------------|-------|
| ריק / שורה ריקה | דילוג |
| `מצבת` | **לשמור בנפרד** — מספר הייחוס שבסוגריים (ראה §3.2) |
| `סה"כ עיר` | **לדלג בטעינה.** משמש רק לאימות ידני מול החישוב העצמאי |

בתי הספר תופסים את השורות עד ~128. הכלל: כל שורה עם שם בית ספר בעמודה 0 שאינו `מצבת` / `סה"כ עיר` — היא שורת בית ספר.

---

## 3. חוקי חישוב

### 3.1 כלל ברזל — חישוב עצמאי
**Claude מחשב הכל בעצמו מהנתונים הגולמיים של בתי הספר.**
שורות `סה"כ עיר` שבגיליון **אינן** מקור לחישוב — לכל היותר כלי הצלבה בדיבוג.

### 3.2 שני מכנים
| מקור | ערכים (י'/יא'/יב') | שימוש |
|------|---------------------|-------|
| `כמות תלמידים` (סכום בתי הספר) | 2108 / 2174 / 1952 | **המכנה הקובע** — מה שהעיר מכירה |
| `מצבת` (משרד החינוך) | 2219 / 2316 / 2105 | **מספר ייחוס** — מוצג בסוגריים ליד המדד |

**ההחלטה הסופית (v12) — אין מתג. תצוגה כפולה במקומו.**

המתג נבנה ב-v10 ונמחק ב-v12 אחרי שהתצוגה הכפולה הוכחה כברורה מספיק.

- **המכנה הקובע הוא תמיד `כמות תלמידים`** — סכום שורות בתי הספר. כל מדד
  עירוני, בכל התצוגות, מחושב עליו. אין מצב תצוגה שני ואין מה לשמור בין סשנים.
- **המצבת היא מספר ייחוס**, מוצגת בסוגריים ליד המספר הראשי בכתב קטן ובהיר
  (`.alt-num`): `18.3% (מצבת 17.5%)`. חל על שלושת ה-KPI לפי שכבה, על הנשירה
  העירונית, על הממוצע העירוני שבכרטיסי מובילים/לקידום ועל גלולת "ממוצע עירוני
  משוקלל" שבהשוואת בתי הספר. תת-הכותרת מציגה את שני המכנים
  (`121 מתוך 663 תלמידים (מצבת 690)`).
- **המונה לעולם אינו משתנה** — תמיד `זכאות למדד טק` מבתי הספר.
- **מדדים בית-ספריים, דירוג, נשירה בית-ספרית ופילוח מגדרי אינם נוגעים למצבת** —
  אין שורת מצבת פר בית ספר, ובפילוח המגדרי המכנה הוא לומדי המקצוע.
- **שכבה בלי ערך מצבת פשוט לא מציגה סוגריים.** זו גם הדרך לראות במבט אחד
  לאילו שכבות יש מצבת בגיליון.

בקוד: `cityDenomPair(שכבה)` מחזירה את שני המכנים · `cityDenomByName` את הקובע ·
`cityDenomAlt` את המצבת (או `null`) · `cityPctAlt` / `cityOverallPctAlt` את המדד
עליה · `altSpan(pct)` את ה-`<span>` שבסוגריים.

### 3.3 נוסחאות

```
// לכל בית ספר × שכבה
techCount   = row('זכאות למדד טק')[grade].total
totalCount  = row('כמות תלמידים')[grade].total
techPct     = techCount / totalCount * 100

// מדד בית ספר (משוקלל על כל השכבות הזמינות)
schoolPct   = Σ techCount / Σ totalCount * 100

// מדד עירוני לשכבה — כל בית ספר שיש לו נתונים באותה שכבה
cityPct(g)  = Σ(schools with data at g) techCount / Σ(same) totalCount * 100
// מספר הייחוס שבסוגריים: אותו מונה, חלקי שורת המצבת של אותה שכבה
cityPctAlt(g) = Σ techCount / matzevet(g) * 100      // null כשאין מצבת לשכבה

// נשירה — י' → יב' בלבד
dropPct     = (techPct(י') - techPct(יב')) / techPct(י') * 100
```

### 3.4 שכבות
**כל השכבות ז'–יב' מוצגות** (שינוי מההחלטה הישנה שהתעלמה מז'–ט').
הנשירה בלבד מחושבת על י'→יב'.

### 3.5 בתי ספר חלקיים — **הכללה לפי שכבה**

מוצגים תמיד, **מסומנים בבירור** ולא מוסתרים. התגיות נגזרות מהנתונים, לא מרשימה קשיחה:

| מצב בגיליון | תגית |
|-------------|------|
| אין אף שכבה מי'–יב' | ⚠️ ז׳–ט׳ בלבד |
| חלק משכבות י'–יב' | ⚠️ י׳ בלבד (וכד' — לפי מה שקיים) |
| אין אף שכבה מז'–ט' | ⚠️ חט״ע |
| כל הבנים או כל הבנות אפס | בנים בלבד / בנות בלבד |

**הכלל (עודכן 12.08.2026 — החליף את הכלל הקודם):**

> **מדדים עירוניים** — כל בית ספר נכלל בכל שכבה שיש לו נתונים עבורה.
> בית ספר עם י' בלבד נספר בשכבה י' ולא בשכבות יא'–יב'.
> זה תקין מתמטית: המדד העירוני הוא יחס של סכומים, ובית ספר חלקי מוסיף גם למונה וגם למכנה באותה שכבה.
>
> **דירוג בין בתי ספר ונשירה** — רק בתי ספר עם **י', יא' ויב' מלאים**.
> כאן הסינון מוצדק: אי אפשר להשוות מדד מצטבר י'–יב' של בית ספר עם שכבה אחת מול בית ספר עם שלוש,
> ואי אפשר לחשב נשירה בלי שני הקצוות.

**הכלל שהוחלף:** קודם *כל* המדדים העירוניים חושבו רק על בתי ספר מלאים. זה הוריד 3 בתי ספר
ו-42 לומדי טק מהתמונה העירונית, והציג מונה "11 בתי ספר" במקום 14.

בקוד: `SCHOOLS` = כל בתי הספר (מדדים עירוניים). `FULL` = בעלי י'–יב' מלאים (דירוג + נשירה בלבד).

### 3.6 עתיד טק — רמזור
מדד: `עתיד טק [סה"כ] / כמות תלמידים [סה"כ]` באותה שכבה (בד"כ ט').

| טווח | צבע | משמעות |
|------|-----|--------|
| > 50% | ירוק `#34a853` | מצוין |
| 30–50% | צהוב `#fbbc04` | טוב |
| < 30% | אדום `#e84235` | דורש שיפור |

תצוגה: לכל בית ספר + חתך עירוני. **האפיון המדויק של הרכיב — בהמשך.**

---

## 4. שפה עיצובית (לשימור מדויק)

```
--primary:   #6a3e9c   סגול חולון
--primary-d: #4a2972
--primary-l: #ede1f7
--accent:    #f47b20   כתום חולון
--good:      #34a853
--warn:      #fbbc04
--bad:       #e84235
--info:      #1a73e8
--bg:        #f5f3f8
--card:      #ffffff
--border:    #e0d8eb
--text:      #1f1530
--muted:     #6a6080
--sidebar:   #2a1a4d
```

- **Heebo** בלבד (400 / 500 / 700 / 900)
- **RTL מלא**
- **Light theme** — אין טקסט לבן על רקע בהיר. לבן מותר רק על ה-sidebar הכהה ועל רקעים צבעוניים רוויים.
- **גדלים להקרנה** — KPI 32px, כותרות 16–24px, טקסט ≥13px
- **Sidebar קבוע** ברוחב 260px, פריט פעיל עם border-right כתום
- כרטיסים: `border-radius:16px`, צל עדין, רצועת צבע עליונה 3px ב-KPI

---

## 5. התצוגות

### קיימות (לא לשנות עיצוב)
1. **סקירה עירונית** — KPIs, בתי ספר מעלים/מורידים, מסע התלמיד
2. **מקצועות** — התפתחות לאורך שכבות, קצב נשירה
3. **השוואת בתי ספר** — כרטיסים + טבלה ממוינת
4. **העמקה לפי בית ספר** — dropdown, KPIs, מסע, פילוח, השוואה

### חדשה (לאפיון בהמשך)
5. **ניתוח השוואתי לאורך תקופות** — ציר זמן על פני כל הטאבים. דורש טעינת **כל** הטאבים.

### עתידיות (מסומנות "בקרוב" בסיידבר)
- פילוח מגדרי
- תחזית רב-שנתית

---

## 6. שכבת הטעינה — מבנה נדרש

```javascript
const PERIODS = [
  { key:'תשפה-2', label:'תשפ"ה סוף',   year:2025, half:2, gid:'...' },
  { key:'תשפו-1', label:'תשפ"ו תחילה', year:2026, half:1, gid:'...' },
  { key:'תשפו-2', label:'תשפ"ו סוף',   year:2026, half:2, gid:'...' },
];

// טוען את כל התקופות במקביל, מפרסר, ומחזיר מבנה אחיד:
// DATA = {
//   'תשפו-2': {
//      schools: { 'אורט': { 'ז': {total, tech, physics, cs, math5, futureTech, boys, girls}, ... } },
//      matzevet: { 'ז': 2141, ... }
//   }, ...
// }
```

**דרישות:**
- טעינה מקבילה (`Promise.all`) של כל הטאבים
- מצב טעינה בממשק (spinner/skeleton) — לא מסך לבן
- טיפול בשגיאה: הודעה ברורה + כפתור ניסיון חוזר
- כפתור **"רענן נתונים"** גלוי, עם חותמת זמן העדכון האחרון
- ברירת מחדל: התקופה **האחרונה** כרונולוגית
- בורר תקופה בממשק (התצוגות 1–4 מציגות תקופה אחת נבחרת)

**פרסינג CSV:** לא לפצל ב-`split(',')` — יש שדות עם פסיקים בתוך מרכאות (`"סה""כ"`). להשתמש ב-PapaParse (כבר זמין) או parser תקין.

---

## 7. סדר עבודה

| # | משימה | סטטוס |
|---|-------|-------|
| 1 | איסוף ה-GID של כל טאב + בניית `PERIODS` | ✅ בוצע — צרוב ב-`GID_MAP` |
| 2 | שכבת טעינה + פרסר | ✅ בוצע |
| 3 | חיבור התצוגות 1–4 ל-`DATA` (ללא נגיעה ב-CSS) | ✅ בוצע |
| 4 | בורר תקופה + כפתור רענון | ✅ בוצע |
| 5 | תיקון הכללת בתי ספר חלקיים (§3.5) | ✅ בוצע |
| 6 | מתג מכנה (כמות תלמידים / מצבת) | ✅ בוצע |
| 7 | אפיון ובניית התצוגה ההשוואתית (5) | ⬜ דורש אפיון |
| 8 | אפיון ובניית רמזור עתיד טק | ⬜ דורש אפיון |

### מצב הפריסה
- קובץ יחיד: `index.html` בריפו `ramishaked/HolonTechDeshboard`, מוגש ב-GitHub Pages.
- אין קבצי נתונים, אין ספריות מקומיות. תלות חיצונית יחידה: Chart.js מ-cdnjs.
- פרסר CSV מובנה בקוד (לא PapaParse).
- מספר גרסה מוצג בסיידבר (`v13`) — לאימות שהקובץ שרץ הוא העדכני.

```javascript
const GID_MAP = {
  'תשפ"ה (סוף שנה)'   : '392061415',
  'תשפ"ו (תחילת שנה)' : '1592440184',
  'תשפ"ו (2)'         : '543820427',   // ← הדגימה הקובעת לתצוגה
};
// טאבים עתידיים שטרם הוזנו: 1320089175, 1235172593
```

---

## 8. ערכי אימות (טאב `תשפ"ו (2)`, gid 543820427)

### 8.1 ערכי גלם — נכונות הפרסר
לא מושפעים משינוי §3.5. אם אלה תואמים, קריאת ה-CSV תקינה.

| בדיקה | ערך צפוי |
|-------|----------|
| אורט — כמות תלמידים ז' | 205 |
| אורט — כמות תלמידים י' | 278 |
| אורט — זכאות למדד טק יב' | 83 |
| קציר — זכאות למדד טק ח' | 197 |
| מצבת — שכבה י' | 2219 |
| מספר בתי ספר בגיליון | 14 |

### 8.2 ערכי צבירה — ⚠️ טעונים מדידה מחדש

הערכים שהיו כאן (2108 / 480 / 22.8% / 21.6% / 24.7%) חושבו לפי **הכלל הישן** —
11 בתי ספר מלאים בלבד. אחרי המעבר להכללה לפי שכבה הם **ישתנו כלפי מעלה**,
כי נוספו בתי ספר חלקיים למונה ולמכנה.

| בדיקה | ערך ישן (11 ב״ס) | ערך חדש (14 ב״ס) |
|-------|------------------|-------------------|
| סכום `כמות תלמידים` י' | 2108 | _למדוד_ |
| סכום `זכאות למדד טק` י' | 480 | _למדוד_ |
| מדד עירוני י' (כמות תלמידים) | 22.8% | _למדוד_ |
| מדד עירוני י' (מצבת) | 21.6% | _למדוד_ |
| סך לומדי טק י'–יב' | 1,245 | 1,287 |
| עתיד טק ט' עירוני | 24.7% 🔴 | _למדוד_ |

**לעדכן את הטבלה מהדשבורד החי לפני ההצגה הבאה לוועדת ההיגוי**,
ולציין בפני הוועדה שהבסיס השתנה — אחרת ייראה כאילו המדד קפץ.

---

## 9. נקודות פתוחות

| נושא | סטטוס |
|------|-------|
| GID לכל טאב | ✅ נאסף וצרוב |
| שמות שני הטאבים העתידיים | לקבל כשיוזנו — נדרש לפענוח התקופה |
| ערכי §8.2 אחרי שינוי הבסיס | למדוד מהדשבורד ולעדכן |
| אפיון התצוגה ההשוואתית | לדון לפני בנייה |
| אפיון רכיב רמזור עתיד טק | לדון לפני בנייה |
| מקום שורת המצבת בממשק | ✅ נסגר — מתג בסיידבר, מעל בורר התקופה (§3.2) |

---

## 10. עקרונות שאסור לשבור

1. **לא להמציא נתונים** — הכל מהגיליון
2. **`זכאות למדד טק`** הוא המדד. לא לסכום פיזיקה+מדמ"ח (יש חפיפה)
3. **חישוב עצמאי** — לא להסתמך על שורות `סה"כ עיר`. כלל זה חל גם על **מספרים
   ושמות בתוך טקסט נרטיבי** — כותרת, תובנה או "קריאה קרה" שנכתבו על תקופה אחת
   ונצרבו בקוד הן אותה תקלה בדיוק (ראה §11, 12.08.2026). גם טענת השוואה
   ("מעל הארצי", "הנמוך בעיר") חייבת להיגזר מהמספר שלידה
4. **בתי ספר חלקיים מסומנים ונכללים** לפי שכבה — לא מוסתרים ולא מסוננים מהמדד העירוני (§3.5)
5. **נשירה = י'→יב'** בלבד
6. **לא לשבור את העיצוב הקיים**

---

## 11. יומן החלטות

| תאריך | החלטה |
|-------|-------|
| 12.08.2026 | מעבר לנתונים חיים מגיליון מפורסם, קובץ יחיד ב-GitHub Pages |
| 12.08.2026 | `GID_MAP` צרוב בקוד — גילוי אוטומטי מ-`pubhtml` אינו אמין (הדף מוגש כמעטפת JS) |
| 12.08.2026 | PapaParse הוחלף בפרסר CSV מובנה — הפחתת תלות חיצונית |
| 12.08.2026 | **§3.5 שונה:** הכללה לפי שכבה במקום סינון בתי ספר חלקיים |
| 12.08.2026 | נוסח: "תמונת מצב בגרות-טק — <תקופה>"; "בתי ספר מובילים" / "בתי ספר לקידום"; תובנה 6 (הזווית המגדרית) נמחקה |
| 12.08.2026 | מבנה הנתונים נשאר מערך של 10 שדות לשכבה (תואם לתצוגות הקיימות), עם 5 שדות נוספים: `ft, ft_g, ma3, ma4, en5` |
| 12.08.2026 | **מתג המכנה (§3.2) מומש.** מיקום: סיידבר, מעל בורר התקופה. היקף: כל מדד עירוני בכל התצוגות — לא רק ה-KPI. המונה אינו משתנה, ומדדים בית-ספריים אינם מושפעים |
| 12.08.2026 | כל המכנים העירוניים אוחדו ל-`cityDenomByName(שכבה)`. `cityOverallPct` ו-`citySubjPctAtGrade` נותבו דרכה במקום לסכום `total` inline — שקול מתמטית במצב ברירת המחדל |
| 12.08.2026 | שכבה בלי ערך מצבת נופלת חזרה לסכום בתי הספר במקום להתאפס. תקופה בלי שורת מצבת — הכפתור מושבת |
| 12.08.2026 | **תצוגה כפולה בסקירה העירונית (v11):** המדד לפי המכנה השני מוצג בסוגריים ליד המספר הראשי, קטן ובהיר. תגית `.d-chip` הוסרה — הסוגריים המתויגות מחליפות אותה |
| 12.08.2026 | **המתג הוסר (v12).** התצוגה הכפולה הספיקה. המכנה הקובע הוא תמיד `כמות תלמידים`; המצבת היא מספר ייחוס בסוגריים. נמחקו `DENOM`, `syncDenomUI`, `setDenom`, `.sb-seg` ומפתח `holon_denom` ב-localStorage. התצוגה הכפולה הורחבה לגלולת הממוצע העירוני בהשוואת בתי הספר |
| 12.08.2026 | **תוקן:** `renderGender` חיפשה את `יבנה` ואת `הראל` בשמם המפורש ועשתה `.grades` בלי הגנה — שם שמשתנה או טאב בלעדיהם הפיל את כל התצוגה. עכשיו בתי הספר החד-מגדריים נגזרים מ-`note` שהפרסר מייצר (§3.5), ו"הקריאה הקרה" מחושבת מהמספרים במקום טקסט קבוע עם `23%` צרוב |
| 12.08.2026 | **תוקן:** `פילוח מגדרי` הופיע פעמיים בסיידבר — כתצוגה פעילה וגם תחת "בקרוב". הכפילות הוסרה |
| 12.08.2026 | הערה בתצוגה המגדרית המסבירה שזיהוי בית ספר חד-מגדרי נגזר מהגיליון (כל עמודות מגדר אחד אפס), ולכן תיאורים מילוליים שאינם בגיליון אינם מוצגים |
| 12.08.2026 | **`renderInsights` שוחררה מ-43 מספרים ו-9 שמות בתי ספר צרובים (v13).** התצוגה תיארה תקופה אחת בלבד ולא הגיבה לבורר התקופה. עכשיו כל מספר נגזר, וגם בחירת בתי הספר: "נקודות חמות" לפי פער מוחלט מול הממוצע העירוני, "מובילים" לפי טק מוחלט / שימור ט׳→י׳ / נשירה מינימלית. בלוקי "פעולה עירונית" נשמרו כלשונם — הם מדיניות, לא נתונים |
| 12.08.2026 | **תוקן:** שתי טענות השוואה היו צרובות ליד מספר חי — "מעל הארצי" בכותרת התובנות ובמסע התלמיד. אם חולון תרד מתחת לארצי הן היו נותרות שגויות. שתיהן נגזרות עכשיו, יחד עם טיפול במקרה שהמסלול גדל במקום להצטמצם |

---

*עודכן: 12 באוגוסט 2026 · גרסת דשבורד v13*
